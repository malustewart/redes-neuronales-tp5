# TP5 Redes neuronales

## Ej 1

Uso:

 - Para generar datos para analizar y almacenarlos en un archivo comprimido llamado `ej1.npz`:

```
python .\ej1.py --generate ej1.npz
```

- Para generar datos para analizar, almacenarlos en un archivo comprimido llamado `ej1.npz` y además analizarlos:

```
python .\ej1.py --generate ej1.npz --analyze
```

- Para analizar datos previamente generados y almacenados en un archivo llamado `ej1.npz`:

```
python .\ej1.py --load ej1.npz --analyze
```